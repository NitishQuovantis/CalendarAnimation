import {StyleSheet} from 'react-native';

export const CalendarPadding = 20;

const styles = StyleSheet.create({
  containerStyle: {
    justifyContent: 'center',
    alignContent: 'center',
    flex: 1,
    padding: CalendarPadding,
    alignItems: 'center',
  },
});

export default styles;
